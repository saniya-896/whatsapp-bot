services:
  - type: web
    name: whatsapp-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y ffmpeg
    startCommand: gunicorn voice:app
